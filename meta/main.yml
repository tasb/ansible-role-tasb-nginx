galaxy_info:
  author: Your Name
  description: Installs Nginx and configures custom initial page and additional port.
  license: MIT
  min_ansible_version: "2.10"
  platforms:
    - name: Debian
      versions:
        - buster
    - name: Rocky
      versions:
        - all
  galaxy_tags:
    - nginx


dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
